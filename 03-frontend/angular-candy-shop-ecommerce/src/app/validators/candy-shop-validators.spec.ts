import { CandyShopValidators } from './candy-shop-validators';

describe('CandyShopValidators', () => {
  it('should create an instance', () => {
    expect(new CandyShopValidators()).toBeTruthy();
  });
});
